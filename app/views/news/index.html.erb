<% @news.each do |news| %>
    <div class="news" id="<%= news.title %>">
    <h2><%= news.title %></h2>
    <%= raw(markdown(news.text)) %>
    <ul>
    <li><%= link_to 'Visa', news %></li>
    <li><%= link_to 'Ändra', edit_news_path(news) %></li>
    <li><%= link_to 'Förinta', news, :confirm => 'Är du säker?', :method => :delete %></li>
    </ul>
    </div>
<% end %>
</table>

<br />

<h3><%= link_to 'Skriv en ny nyhet', new_news_path %></h3>
