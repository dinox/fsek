<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h2><%= @news.title %></h2>

<p><em>Postat av <%= link_to @news.user.real_name, @news.user %>
den <%= l @news.created_at.to_date %></em></p>

<%= raw(markdown(@news.text)) %>

<%= controls @news, :p => true, :back => news_index_path %>
